  test:
    sequence:
##### Общее потребление
    - service: mqtt.publish
      data_template:
        topic: "test/mesage"
        payload: "{{ states('sensor.time')}}"
        retain: true
        
  purge:
    sequence:        
     - service: recorder.purge
       data:
          keep_days: 1
          repack: true